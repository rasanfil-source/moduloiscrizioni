name: Sanitization check

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  sanitization:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Scan public files
        shell: pwsh
        run: ./tools/check-sanitization.ps1
